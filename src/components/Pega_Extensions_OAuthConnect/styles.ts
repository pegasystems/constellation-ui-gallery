import { CardHeader, CardContent } from '@pega/cosmos-react-core';
import styled, { css } from 'styled-components';

export const StyledSummaryListHeader = styled(CardHeader)(() => css``);

export const StyledSummaryListContent = styled(CardContent)(() => css``);
