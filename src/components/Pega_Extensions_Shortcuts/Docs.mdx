import { Meta, Primary, Controls, Story } from '@storybook/addon-docs/blocks';
import * as DemoStories from './demo.stories';

<Meta of={DemoStories} />

# Support

This component is **deprecated** and may be removed in a future release of the UI Gallery. In Pega '26, you can use the out-of-the-box Shortcuts widget.

**It is strongly recommended to remove this component from your application** and switch to the out-of-the-box Shortcuts widget.

# Overview

The Shortcuts utility renders a list of links inside a card - Each link will open a page inside your application or could open an external URL.

The widget can render the links on a single row (displayType = 'simple') or group them by category. When using the 'grouped' displayType, a JSON object must be passed as a string.

<Primary />

## Props

<Controls />
