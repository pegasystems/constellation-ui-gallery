import styled, { css } from 'styled-components';

export default styled.div(() => {
  return css`
    border: 1px solid #ccc;
    padding: 4px;
    width: 100%;
    overflow: hidden;
    white-space: normal;
  `;
});
