import { Meta, Primary, Controls, Story } from '@storybook/addon-docs/blocks';
import * as DemoStories from './demo.stories';

<Meta of={DemoStories} />

# Support

This component provides **limited support**. It is read-only and is not intended as full support for the CPQ application.

# Overview

This component reads the CPQ Tree structure and displays it in Constellation.

<Primary />

## Props

<Controls />
