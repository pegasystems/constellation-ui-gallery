import { Meta, Primary, Controls, Story } from '@storybook/addon-docs/blocks';
import * as DemoStories from './demo.stories';

<Meta of={DemoStories} />

# Overview

This component displays a stepper with naviation functionality inside screen flow, allowing users to move through each step in the screen flow by clicking on the respective step.

Note: To apply this custom component to the screen flow, it must be added to each step individually.

<Primary />

## Props

<Controls />
